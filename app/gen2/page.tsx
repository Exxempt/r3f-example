import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '3D',
  description: ''
};

export default async function BlogPage() {

  return (
    <section>
      <h1 className="font-bold text-2xl mb-8 tracking-tighter">Enter monke # </h1>
    </section>
  );
}
